[package]
name = "redis"
version = "0.1.0"
edition = "2024"

[dependencies]
# Match the version that deadpool-redis 0.14 uses
redis = { version = "0.24", features = ["tokio-comp"] }
deadpool-redis = "0.14"
tokio = { version = "1", features = ["full"] }
